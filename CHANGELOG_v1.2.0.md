# FluxDown Cover v1.2.0 更新日志

## 新增功能
- 新建下载改为独立弹窗（Stack 叠加层），所有设备统一，与窗口缩放完全独立
- 弹窗弹性动画：从 FAB 加号位置展开/缩回，带过冲回弹效果
- 任务详情分段进度可视化（绿=完成/蓝=下载中/灰=未开始）
- 任务时间分组（今天/昨天/更早）
- 任务"移动"操作
- 做种状态 Tab（原版6个Tab补齐）
- 内置文件分类（视频/音频/图片/文档/压缩包/APK）
- 筛选弹窗三分区（分类/队列/排序）
- 排序选项（最新创建/按大小/按名称/按优先级）

## 修复
- 设置选项点击即时反馈（文件已存在策略、文件丢失处理、BT条件组合立即高亮）
- 新建下载弹窗稳定打开（修复 bindSheet 在 PC 端 Split 模式下不生效）
- BT 数字输入框数字裁切（宽度72→90，高度30→36）
- 子页面标题栏背景色与内容区统一
- 返回箭头图标修复（之前引用不存在的图标名导致显示错误）
- 弹窗关闭动画与打开一致，都有弹性回弹

## UI 优化
- 所有子页面自定义标题栏（返回/关闭图标+标题），背景与内容区一致
- 新建下载弹窗手机端缩小居中（92%×82%），PC/平板保持原尺寸
- 弹窗四角圆角统一（之前上下圆角不一致）
- 批量下载输入框圆角与下载链接统一（8→12）
- 顶部状态栏保持 APP 原色，底部导航栏灰色蒙层
- 全局转场动画（页面切换滑入淡入、Tab 切换平滑过渡）
- 深色模式配色与原版 FluxDown 一致

## 性能优化
- 设置加载并行化（20项串行读取→Promise.all并行）
- 任务恢复+数据库初始化+BT设置 5路并行加载
- 后台服务延迟300ms启动（RSS/剪贴板/队列调度），不阻塞首屏
- 深链轮询降频（800ms→1500ms）
