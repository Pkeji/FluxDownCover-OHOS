import { appTasks } from '@ohos/hvigor-ohos-plugin';

export default {
  system: appTasks,  // Function to obtain the task list for building the app.
  plugins: []        // Custom plugin to extend the build. Built-in plugins are not required.
}
